package cse_142.cs1_exams.finals;

/**
 * https://practiceit.cs.washington.edu/problem/view/cs1/exams/finals/final6/bound
 *
 * The bound method makes sure that this Date object is between
 * d1's and d2's dates, inclusive.
 * If this Date object is not between those dates inclusive,
 * it is adjusted to the nearest date in the acceptable range.
 * The method returns a result of true if this Date was within
 * the acceptable range, or false if it was shifted.
 *
 * @author Alex Golub
 * @since 14-Apr-21 8:33 PM
 */
class bound {
//  public boolean bound(Date d1, Date d2) {
//    if (month < d1.month || (month == d1.month && day < d1.day)) {
//      // adjusting to left bound month
//      month = d1.month;
//      day = d1.day;
//      return false;
//    }
//
//    if (month > d2.month || (month == d2.month && day > d2.day)) {
//      // adjusting to right bound month
//      month = d2.month;
//      day = d2.day;
//      return false;
//    }
//
//    return true;
//  }
}
